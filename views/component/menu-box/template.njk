{% from "button/macro.njk" import govukButton %}

<div class="right-menu">
    <h4 class="govuk-heading-s">{{ boxTitle }}</h4>
    <a class="govuk-link menu-link" href="/content-management/add-course">Add a new course</a>

    {% if (delete === true} %}
        {{ govukButton({
            text: "Delete",
            "classes": "govuk-button--delete"
        }) }}
    {% endif %}
</div>