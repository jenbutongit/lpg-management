<ul class="list">
    {% for learningProvider in pageResults.results %}
        {# {% set providers = [
            {
                learningProvider.name
            }
        ] %}
        <p>{{providers}}</p> #}
        <p class="govuk-body">{{learningProvider.name}}</p>
    {% endfor %}
</ul>
